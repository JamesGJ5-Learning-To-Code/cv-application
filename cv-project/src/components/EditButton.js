import React, { Component } from 'react';

class EditButton extends Component {
    render () {
        return (
            <button>
                Edit
            </button>
        )
    }
}

export default EditButton
